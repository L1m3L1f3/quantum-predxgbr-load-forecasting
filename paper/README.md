# Final Paper Package

Main manuscript:

- `final_paper.tex`
- `references.bib`

Generated comparison artifacts are in:

- `../results/paper_comparison/`
- `../figures/`

Main result:

The full proposed pipeline beats the PredXGBR paper baseline on all five datasets. The strongest model is the best sequential residual postprocess. Hybrid quantum-enhanced residual features improve over local XGBoost in selected cases, but the improvement is dataset-dependent.
